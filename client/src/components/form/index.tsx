import React from 'react';

import { FormContainer } from './form.styles';

export const Form: React.FC = () => {
    return (
        <FormContainer>
            TO DO
        </FormContainer>
    )
}